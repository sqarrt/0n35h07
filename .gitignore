# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
steam-build
*.local

# Icon preview renders (generated from SVG) — we keep the svg/html sources, not the png
design/icon-concepts/*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.playwright-mcp
test-results
.env

# Strudel prelude for the music editor (AGPL neighbor) — not bundled into the game
prebake.strudel

# Local Claude settings/plans and debug screenshots
.claude/
dash-strips*.png
docs/
TODO.md
.superpowers
.mcp.json

# Map editor per-session backup (autosave artifact — not shipped)
src/maps/*/backup.json

# Steamworks SDK — vendored locally for builds; not redistributable in a public repo (Valve license)
steamworks_sdk_164/

# Session debug artifacts (perf traces, network __netReport, screenshots)
Trace-*.json.gz
*netreport*
piska*.json
localhost_*.png
