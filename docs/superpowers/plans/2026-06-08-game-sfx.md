# Игровые SFX — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Подключить 18 присланных SFX в игру: 3D-позиционно для геймплея, 2D для UI/отсчёта, лупы для щита.

**Architecture:** Один `ISfxEngine` (`ThreeSfxEngine` на `THREE.AudioListener`) живёт на уровне `App` (один AudioContext, доступен в меню и матче). В матче listener цепляется к камере, позиционные ноды — в `match.root`. Логика триггеров — в `MatchSfx` (тестируется через `FakeSfxEngine`), который `Match` дёргает: боёвка — из пути событий, движения — из локальной симуляции + новое событие `move` для соперника, UI — через `useSfx()` в React.

**Tech Stack:** three.js audio (`AudioListener`/`PositionalAudio`/`Audio`), Web Audio `decodeAudioData`, React context, Vitest (логика через фейк), Playwright (e2e-смоук). `erasableSyntaxOnly` — без enum/parameter-properties.

**Спека:** `docs/superpowers/specs/2026-06-08-game-sfx-design.md`

---

## Контекст для исполнителя

- SFX-файлы лежат в соседнем репо: `C:\Users\Home\PycharmProjects\oneshot_music_editor\render_opus\game_sfx\*.opus` (18 шт.).
- Музыка — отдельная подсистема (`src/game/audio/`), её не трогаем.
- `Match` (`src/game/Match.ts`): host-путь `update` (строки ~228-237: `controllers→players.update→applyPhysics→resolveCombat→resolveRespawns→clock→hud→lateUpdate`); client-путь (~210-224: свой предсказан, удалённые из снапшотов). `emit(e)` — host-only (гард внутри). `applyEvent(e)` (~517-561) — client применяет события. `applySnapshot` (~507-514) — client применяет снапшоты (`applyNetState` для удалённых). Поля `localId`, `byId: Map<number,Player>`, `players: Player[]`, `human`, `root: THREE.Group`.
- `Player` (`src/game/Player.ts`): `position`, `bodyGroup`, `shieldActive`, `dashing`, `grounded`, `weaponJustFired`, `dashCooldownProgress()`, `shieldProgress()`.
- `Body` (`src/game/Body.ts`): `stepJump()` применяет прыжок (земля и воздух).
- Команды: юниты `npx vitest run --config vitest.config.ts <файл>`; типы `npx tsc -b --noEmit`; e2e `npx playwright test --project=headless <файл>`; всё `npm run test`.

---

## Task 1: ассеты, манифест, типы, ISfxEngine, FakeSfxEngine

**Files:**
- Copy: `oneshot_music_editor/render_opus/game_sfx/*.opus` → `src/assets/sfx/`
- Create: `src/game/audio/sfx/types.ts`, `src/game/audio/sfx/sfxLibrary.ts`, `src/game/audio/sfx/FakeSfxEngine.ts`
- Test: `tests/unit/sfxLibrary.test.ts`

- [ ] **Step 1: Скопировать файлы**

```bash
mkdir -p "C:/Users/Home/PycharmProjects/OneShot/src/assets/sfx"
cp "C:/Users/Home/PycharmProjects/oneshot_music_editor/render_opus/game_sfx/"*.opus "C:/Users/Home/PycharmProjects/OneShot/src/assets/sfx/"
ls "C:/Users/Home/PycharmProjects/OneShot/src/assets/sfx/" | wc -l
```
Expected: 18

- [ ] **Step 2: Написать падающий тест манифеста**

Create `tests/unit/sfxLibrary.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { SFX_LIBRARY } from '../../src/game/audio/sfx/sfxLibrary'

describe('SFX_LIBRARY', () => {
  it('содержит все 18 событий с url', () => {
    const ids = Object.keys(SFX_LIBRARY).sort()
    expect(ids).toContain('beam_fire')
    expect(ids).toContain('shield_loop')
    expect(ids).toContain('count_tick')
    expect(ids.length).toBe(18)
    for (const url of Object.values(SFX_LIBRARY)) expect(typeof url).toBe('string')
  })
})
```

- [ ] **Step 3: Запустить — упадёт**

Run: `npx vitest run --config vitest.config.ts tests/unit/sfxLibrary.test.ts`
Expected: FAIL (модуль не найден).

- [ ] **Step 4: Создать типы**

Create `src/game/audio/sfx/types.ts`:

```ts
import type * as THREE from 'three'

/** id события SFX = имя файла без расширения. */
export type SfxEvent =
  | 'beam_fire' | 'block' | 'shield_up' | 'shield_down' | 'cooldown_ready'
  | 'dash' | 'jump' | 'land' | 'death' | 'respawn'
  | 'ui_click' | 'ui_hover' | 'ui_toggle' | 'ready' | 'lobby_join' | 'count_tick'
  | 'shield_loop' | 'ghost_loop'

/** Движок SFX (DIP-граница: реальный three-аудио ИЛИ фейк в тестах). */
export interface ISfxEngine {
  load(): Promise<void>
  /** На входе в матч: listener на камеру, parent (match.root) для позиционных нод. */
  attach(camera: THREE.Camera, parent: THREE.Object3D): void
  detach(): void
  playAt(event: SfxEvent, pos: THREE.Vector3, gain?: number): void
  play2D(event: SfxEvent, gain?: number): void
  startLoop(event: SfxEvent, key: string, target: THREE.Object3D): void
  stopLoop(key: string): void
  setMasterGain(gain: number): void
  dispose(): void
}
```

- [ ] **Step 5: Создать манифест**

Create `src/game/audio/sfx/sfxLibrary.ts`:

```ts
import type { SfxEvent } from './types'

// Eager-манифест: путь → url ассета (Vite). Имя файла без .opus = id события.
const modules = import.meta.glob('../../../assets/sfx/*.opus', { eager: true, query: '?url', import: 'default' })

function buildLibrary(): Record<SfxEvent, string> {
  const lib = {} as Record<SfxEvent, string>
  for (const [path, url] of Object.entries(modules)) {
    const id = path.split('/').pop()!.replace('.opus', '') as SfxEvent
    lib[id] = url as string
  }
  return lib
}

export const SFX_LIBRARY: Record<SfxEvent, string> = buildLibrary()
```

- [ ] **Step 6: Создать FakeSfxEngine**

Create `src/game/audio/sfx/FakeSfxEngine.ts`:

```ts
import type { ISfxEngine, SfxEvent } from './types'

/** Тестовый двойник: записывает вызовы (three-аудио в jsdom не работает). */
export class FakeSfxEngine implements ISfxEngine {
  calls: { method: string; event?: SfxEvent; key?: string }[] = []
  async load() {}
  attach() {}
  detach() {}
  playAt(event: SfxEvent) { this.calls.push({ method: 'playAt', event }) }
  play2D(event: SfxEvent) { this.calls.push({ method: 'play2D', event }) }
  startLoop(event: SfxEvent, key: string) { this.calls.push({ method: 'startLoop', event, key }) }
  stopLoop(key: string) { this.calls.push({ method: 'stopLoop', key }) }
  setMasterGain() {}
  dispose() {}
  /** Сколько раз сыграно событие (любым методом). */
  played(event: SfxEvent) { return this.calls.filter(c => c.event === event).length }
  clear() { this.calls = [] }
}
```

- [ ] **Step 7: Запустить — зелёный**

Run: `npx vitest run --config vitest.config.ts tests/unit/sfxLibrary.test.ts`
Expected: PASS.

- [ ] **Step 8: Коммит**

```bash
git add src/assets/sfx src/game/audio/sfx/types.ts src/game/audio/sfx/sfxLibrary.ts src/game/audio/sfx/FakeSfxEngine.ts tests/unit/sfxLibrary.test.ts
git commit -m "feat(sfx): ассеты, манифест, ISfxEngine, FakeSfxEngine"
```

---

## Task 2: ThreeSfxEngine (реальный движок)

**Files:**
- Create: `src/game/audio/sfx/ThreeSfxEngine.ts`

(Тестируется e2e в Task 6 — three-аудио в jsdom не идёт. Здесь только реализация + проверка типов.)

- [ ] **Step 1: Создать движок**

Create `src/game/audio/sfx/ThreeSfxEngine.ts`:

```ts
import * as THREE from 'three'
import { SFX_LIBRARY } from './sfxLibrary'
import type { ISfxEngine, SfxEvent } from './types'

const SFX_MASTER_GAIN = 0.9
const SFX_REF_DISTANCE = 6
const SFX_ROLLOFF = 1
const SFX_MAX_DISTANCE = 60
// Громкости per-event (звуки НЕ нормализованы): UI заметно тише геймплея.
const SFX_GAIN: Record<SfxEvent, number> = {
  beam_fire: 0.9, block: 0.9, shield_up: 0.8, shield_down: 0.8, cooldown_ready: 0.5,
  dash: 0.8, jump: 0.7, land: 0.6, death: 1.0, respawn: 0.9,
  ui_click: 0.4, ui_hover: 0.3, ui_toggle: 0.4, ready: 0.5, lobby_join: 0.5, count_tick: 0.6,
  shield_loop: 0.5, ghost_loop: 0.5,
}

/** SFX-движок на three.js audio. Один listener (один AudioContext) на всё приложение. */
export class ThreeSfxEngine implements ISfxEngine {
  private listener = new THREE.AudioListener()
  private buffers = new Map<SfxEvent, AudioBuffer>()
  private parent: THREE.Object3D | null = null
  private loops = new Map<string, THREE.PositionalAudio>()

  constructor() { this.listener.setMasterVolume(SFX_MASTER_GAIN) }

  async load(): Promise<void> {
    const ctx = this.listener.context
    const entries = Object.entries(SFX_LIBRARY) as [SfxEvent, string][]
    await Promise.all(entries.map(async ([id, url]) => {
      if (this.buffers.has(id)) return
      const data = await (await fetch(url)).arrayBuffer()
      this.buffers.set(id, await ctx.decodeAudioData(data))
    }))
  }

  attach(camera: THREE.Camera, parent: THREE.Object3D): void {
    camera.add(this.listener)
    this.parent = parent
    this.resume()
  }
  detach(): void {
    for (const key of [...this.loops.keys()]) this.stopLoop(key)
    this.listener.removeFromParent()
    this.parent = null
  }

  playAt(event: SfxEvent, pos: THREE.Vector3, gain = 1): void {
    const buf = this.buffers.get(event)
    if (!buf || !this.parent) return
    this.resume()
    const a = new THREE.PositionalAudio(this.listener)
    a.setBuffer(buf)
    a.setRefDistance(SFX_REF_DISTANCE)
    a.setRolloffFactor(SFX_ROLLOFF)
    a.setMaxDistance(SFX_MAX_DISTANCE)
    a.setVolume((SFX_GAIN[event] ?? 1) * gain)
    a.position.copy(pos)
    this.parent.add(a)
    a.updateMatrixWorld()
    a.onEnded = () => { a.removeFromParent(); a.disconnect() }
    a.play()
  }

  play2D(event: SfxEvent, gain = 1): void {
    const buf = this.buffers.get(event)
    if (!buf) return
    this.resume()
    const a = new THREE.Audio(this.listener)
    a.setBuffer(buf)
    a.setVolume((SFX_GAIN[event] ?? 1) * gain)
    a.onEnded = () => { a.disconnect() }
    a.play()
  }

  startLoop(event: SfxEvent, key: string, target: THREE.Object3D): void {
    if (this.loops.has(key)) return
    const buf = this.buffers.get(event)
    if (!buf) return
    this.resume()
    const a = new THREE.PositionalAudio(this.listener)
    a.setBuffer(buf)
    a.setRefDistance(SFX_REF_DISTANCE)
    a.setRolloffFactor(SFX_ROLLOFF)
    a.setMaxDistance(SFX_MAX_DISTANCE)
    a.setLoop(true)
    a.setVolume(SFX_GAIN[event] ?? 0.5)
    target.add(a)
    a.play()
    this.loops.set(key, a)
  }
  stopLoop(key: string): void {
    const a = this.loops.get(key)
    if (!a) return
    try { a.stop() } catch { /* источник мог не стартовать */ }
    a.removeFromParent()
    a.disconnect()
    this.loops.delete(key)
  }

  setMasterGain(gain: number): void { this.listener.setMasterVolume(gain) }
  dispose(): void { this.detach(); this.buffers.clear() }

  private resume(): void {
    if (this.listener.context.state === 'suspended') void this.listener.context.resume()
  }
}
```

- [ ] **Step 2: Проверить типы**

Run: `npx tsc -b --noEmit`
Expected: без ошибок.

- [ ] **Step 3: Коммит**

```bash
git add src/game/audio/sfx/ThreeSfxEngine.ts
git commit -m "feat(sfx): ThreeSfxEngine (three.js audio, позиц.+2D+лупы)"
```

---

## Task 3: проводка — App создаёт движок, контекст useSfx, Game attach, инжект в Match, UI-клики кнопок

**Files:**
- Create: `src/sfx/SfxContext.tsx`
- Modify: `src/App.tsx`, `src/Game.tsx`, `src/game/Match.ts`, `src/ui/Button.tsx`
- Test: `tests/unit/Match.sfx.test.ts`

- [ ] **Step 1: Падающий тест инжекта в Match**

Create `tests/unit/Match.sfx.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import * as THREE from 'three'
import { Match } from '../../src/game/Match'
import { FakeSfxEngine } from '../../src/game/audio/sfx/FakeSfxEngine'
import type { RosterEntry } from '../../src/net/protocol'

function makeMatch(sfx?: FakeSfxEngine) {
  const scene = new THREE.Scene()
  const camera = new THREE.PerspectiveCamera(75, 1, 0.1, 200)
  const controls = { current: { pointerSpeed: 1 } }
  const keys = { current: { forward: false, back: false, left: false, right: false } }
  const roster: RosterEntry[] = [
    { id: 0, name: 'Вы', color: '#4af', kind: 'human' },
    { id: 1, name: 'Бот', color: '#5af', kind: 'bot', difficulty: 'passive' },
  ]
  return new Match({
    scene, camera, controls: controls as any, keys: keys as any, dispatch: vi.fn(),
    role: 'host', netConfig: { localId: 0, roster }, sfxEngine: sfx,
  })
}

describe('Match × SFX', () => {
  it('без sfxEngine update не падает', () => {
    const m = makeMatch()
    m.forceLiveForTest()
    expect(() => m.update(0.016)).not.toThrow()
  })

  it('с sfxEngine конструируется без ошибок', () => {
    expect(() => makeMatch(new FakeSfxEngine())).not.toThrow()
  })
})
```

- [ ] **Step 2: Запустить — упадёт**

Run: `npx vitest run --config vitest.config.ts tests/unit/Match.sfx.test.ts`
Expected: FAIL (`sfxEngine` не в `MatchOptions`).

- [ ] **Step 3: Добавить MatchSfx-заглушку и инжект в Match**

Create `src/game/audio/sfx/MatchSfx.ts` (наполнится в Task 4-5; пока пустой каркас):

```ts
import type { ISfxEngine } from './types'

/** Логика триггеров SFX матча. Единственное место правил «событие/переход → звук». */
export class MatchSfx {
  private engine: ISfxEngine
  constructor(engine: ISfxEngine) { this.engine = engine }
}
```

In `src/game/Match.ts`: add import near the music import:

```ts
import type { ISfxEngine } from './audio/sfx/types'
import { MatchSfx } from './audio/sfx/MatchSfx'
```

In `MatchOptions` interface add field (рядом с `musicEngine?`):

```ts
  sfxEngine?: ISfxEngine
```

Add field on the class (рядом с `private music`):

```ts
  private sfx: MatchSfx | null = null
```

In the constructor, after the music line (`if (o.seedCode && o.musicEngine) ...`):

```ts
    if (o.sfxEngine) this.sfx = new MatchSfx(o.sfxEngine)
```

- [ ] **Step 4: Запустить — зелёный**

Run: `npx vitest run --config vitest.config.ts tests/unit/Match.sfx.test.ts`
Expected: PASS.

- [ ] **Step 5: Контекст useSfx**

Create `src/sfx/SfxContext.tsx`:

```tsx
import { createContext, useContext } from 'react'
import type { ReactNode } from 'react'
import type { ISfxEngine } from '../game/audio/sfx/types'

const SfxCtx = createContext<ISfxEngine | null>(null)

export function SfxProvider({ engine, children }: { engine: ISfxEngine; children: ReactNode }) {
  return <SfxCtx.Provider value={engine}>{children}</SfxCtx.Provider>
}

/** Хук для UI: безопасен вне провайдера (no-op). */
export function useSfx(): Pick<ISfxEngine, 'play2D'> {
  const engine = useContext(SfxCtx)
  return { play2D: (e, g) => engine?.play2D(e, g) }
}
```

- [ ] **Step 6: App создаёт движок, грузит, оборачивает в провайдер, передаёт в Game**

In `src/App.tsx`:

(a) imports:
```ts
import { ThreeSfxEngine } from './game/audio/sfx/ThreeSfxEngine'
import { SfxProvider } from './sfx/SfxContext'
```

(b) В теле компонента `App` (рядом с другими `useRef`/верхними хуками) создать движок один раз и загрузить:
```ts
  const sfxRef = useRef<ThreeSfxEngine | null>(null)
  if (sfxRef.current === null) sfxRef.current = new ThreeSfxEngine()
  const sfx = sfxRef.current
  useEffect(() => { void sfx.load() }, [sfx])
```

(c) Обернуть корневой возвращаемый JSX в `<SfxProvider engine={sfx}>...</SfxProvider>` (на самом внешнем контейнере, чтобы и меню, и игра были внутри).

(d) Туда, где рендерится `<Game ... />`, добавить проп `sfxEngine={sfx}`.

- [ ] **Step 7: Game принимает движок, цепляет listener, инжектит в Match**

In `src/Game.tsx`:

(a) В `GameProps` добавить:
```ts
  sfxEngine: ISfxEngine
```
и импорт типа:
```ts
import type { ISfxEngine } from './game/audio/sfx/types'
```
(b) В сигнатуру `Game({...})` добавить `sfxEngine`.

(c) В `useMemo`-конструкцию `Match` добавить опцию рядом с `musicEngine`:
```ts
      sfxEngine,
```

(d) Прицепить/отцепить listener на маунте — добавить `useEffect` (после существующего debug-`useEffect`):
```ts
  useEffect(() => {
    sfxEngine.attach(camera, match.root)
    return () => sfxEngine.detach()
  }, [camera, match, sfxEngine])
```

- [ ] **Step 8: UI-клик/наведение в Button**

Replace `src/ui/Button.tsx` with:

```tsx
import type { ButtonHTMLAttributes } from 'react'
import { useSfx } from '../sfx/SfxContext'

type Variant = 'primary' | 'secondary' | 'ghost'

const VARIANT_CLASS: Record<Variant, string> = {
  primary: 'btn btn--primary',
  secondary: 'btn',
  ghost: 'btn btn--ghost',
}

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: Variant
}

/** Hard-кнопка проекта. Нативный <button> (роли/disabled для e2e сохранены) + UI-звуки. */
export function Button({ variant = 'secondary', className, onClick, onMouseEnter, ...rest }: ButtonProps) {
  const sfx = useSfx()
  const cls = `${VARIANT_CLASS[variant]}${className ? ` ${className}` : ''}`
  return (
    <button
      className={cls}
      onClick={e => { if (!e.currentTarget.disabled) sfx.play2D('ui_click'); onClick?.(e) }}
      onMouseEnter={e => { if (!e.currentTarget.disabled) sfx.play2D('ui_hover'); onMouseEnter?.(e) }}
      {...rest}
    />
  )
}
```

- [ ] **Step 9: Проверки**

Run: `npx tsc -b --noEmit` → без ошибок.
Run: `npx vitest run --config vitest.config.ts tests/unit/Match.sfx.test.ts` → PASS.
Run: `npx playwright test --project=headless tests/lobby.spec.ts` → PASS (меню/кнопки рендерятся и кликаются без ошибок; SFX-движок в провайдере не ломает DOM).

- [ ] **Step 10: Коммит**

```bash
git add src/sfx/SfxContext.tsx src/game/audio/sfx/MatchSfx.ts src/App.tsx src/Game.tsx src/game/Match.ts src/ui/Button.tsx tests/unit/Match.sfx.test.ts
git commit -m "feat(sfx): проводка движка (App→Game→Match), useSfx, клики кнопок"
```

---

## Task 4: боёвка — beam_fire / block / death / respawn

**Files:**
- Modify: `src/game/audio/sfx/MatchSfx.ts`, `src/game/Match.ts`
- Test: `tests/unit/MatchSfx.test.ts`

- [ ] **Step 1: Падающий тест combat()**

Create `tests/unit/MatchSfx.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import * as THREE from 'three'
import { MatchSfx } from '../../src/game/audio/sfx/MatchSfx'
import { FakeSfxEngine } from '../../src/game/audio/sfx/FakeSfxEngine'

const pos = () => new THREE.Vector3(1, 2, 3)

describe('MatchSfx.combat', () => {
  it('fired→beam_fire, block→block, kill→death, respawn→respawn', () => {
    const fake = new FakeSfxEngine()
    const sfx = new MatchSfx(fake)
    sfx.combat({ t: 'fired', id: 1, end: [0, 0, 0], hitPoint: null, hit: null }, () => pos())
    sfx.combat({ t: 'block', shooter: 0, victim: 1 }, () => pos())
    sfx.combat({ t: 'kill', shooter: 0, victim: 1 }, () => pos())
    sfx.combat({ t: 'respawn', id: 1, pos: [0, 1, 0] }, () => pos())
    expect(fake.played('beam_fire')).toBe(1)
    expect(fake.played('block')).toBe(1)
    expect(fake.played('death')).toBe(1)
    expect(fake.played('respawn')).toBe(1)
  })

  it('игнорирует не-боевые события (scores/time)', () => {
    const fake = new FakeSfxEngine()
    new MatchSfx(fake).combat({ t: 'time', remainingMs: 1000 }, () => pos())
    expect(fake.calls.length).toBe(0)
  })
})
```

- [ ] **Step 2: Запустить — упадёт**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts`
Expected: FAIL (`combat` не определён).

- [ ] **Step 3: Реализовать combat()**

Replace `src/game/audio/sfx/MatchSfx.ts`:

```ts
import * as THREE from 'three'
import type { ISfxEngine } from './types'
import type { MatchEvent } from '../../../net/protocol'

/** Логика триггеров SFX матча. Единственное место правил «событие/переход → звук». */
export class MatchSfx {
  private engine: ISfxEngine
  constructor(engine: ISfxEngine) { this.engine = engine }

  /** Боёвка — из общего пути событий (host: emit; client: applyEvent). posOf даёт мир-позицию игрока. */
  combat(e: MatchEvent, posOf: (id: number) => THREE.Vector3 | null): void {
    switch (e.t) {
      case 'fired':   { const p = posOf(e.id);     if (p) this.engine.playAt('beam_fire', p); break }
      case 'block':   { const p = posOf(e.victim); if (p) this.engine.playAt('block', p);     break }
      case 'kill':    { const p = posOf(e.victim); if (p) this.engine.playAt('death', p);      break }
      case 'respawn': { this.engine.playAt('respawn', new THREE.Vector3(e.pos[0], e.pos[1], e.pos[2])); break }
      default: break
    }
  }
}
```

- [ ] **Step 4: Запустить — зелёный**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts`
Expected: PASS.

- [ ] **Step 5: Подключить в Match — host (emit) и client (applyEvent), + свой выстрел клиента**

In `src/game/Match.ts`:

(a) Хелпер позиции игрока — добавить приватный метод рядом с `musicRemainingMs`:
```ts
  private sfxPos = (id: number): THREE.Vector3 | null => this.byId.get(id)?.position ?? null
```

(b) **Host:** в методе `emit(e)` — после отправки события в сеть добавить (combat сам фильтрует по типу):
```ts
    this.sfx?.combat(e, this.sfxPos)
```
(найди тело `emit` — это строки, где событие шлётся в транспорт; добавь вызов в конце метода, чтобы хост озвучивал боёвку обоих игроков.)

(c) **Client:** в `applyEvent(e)` — в кейсах `kill`, `block`, `respawn` добавить SFX, а для `fired` — только для соперника (свой выстрел клиент озвучивает предсказанием, см. (d)):
```ts
      case 'fired': {
        if (e.id === this.localId) break   // свой выстрел уже показан И озвучен предсказанием
        const hideImpact = e.hit === this.localId && !this.human.bodyIsVisible
        const hp = e.hitPoint && !hideImpact ? fromVec3(e.hitPoint) : null
        this.byId.get(e.id)?.cosmeticFire(fromVec3(e.end), hp)
        this.sfx?.combat(e, this.sfxPos)   // выстрел соперника — позиционно
        break
      }
```
для `kill`/`block`/`respawn` добавь строку `this.sfx?.combat(e, this.sfxPos)` внутри соответствующего кейса (после существующей логики).

(d) **Свой выстрел клиента** (предсказание): в client-ветке `update` (~строка 216) рядом с `p.clearJustFired()`:
```ts
        if (p.id === this.localId) {
          p.update(dt, this.world, this.excludeIds(p))
          if (p.weaponJustFired) this.sfx?.playAtSelf(p)   // свой выстрел — сразу, синхронно с визуалом
          p.clearJustFired()
        } else {
```
и добавить в `MatchSfx` метод:
```ts
  /** Свой выстрел (клиент, предсказание) — позиционно у себя. */
  playAtSelf(player: { position: THREE.Vector3 }): void { this.engine.playAt('beam_fire', player.position) }
```

- [ ] **Step 6: Проверки**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts tests/unit/Match.sfx.test.ts` → PASS.
Run: `npx tsc -b --noEmit` → без ошибок.

- [ ] **Step 7: Коммит**

```bash
git add src/game/audio/sfx/MatchSfx.ts src/game/Match.ts tests/unit/MatchSfx.test.ts
git commit -m "feat(sfx): боёвка — beam_fire/block/death/respawn (host emit + client apply)"
```

---

## Task 5: движения — прыжок/рывок/щит/land/cooldown + событие move + отсчёт

**Files:**
- Modify: `src/game/Body.ts`, `src/game/Player.ts`, `src/net/protocol.ts`, `src/game/audio/sfx/MatchSfx.ts`, `src/game/Match.ts`
- Test: `tests/unit/MatchSfx.test.ts`

- [ ] **Step 1: Падающий тест frame()**

Add to `tests/unit/MatchSfx.test.ts`:

```ts
import type { PlayerSfxInput } from '../../src/game/audio/sfx/MatchSfx'

function input(over: Partial<PlayerSfxInput> = {}): PlayerSfxInput {
  return {
    id: 1, obj: new THREE.Object3D(), pos: new THREE.Vector3(),
    shieldActive: false, dashing: false, grounded: true, justJumped: false,
    dashReady: null, shieldReady: null, ...over,
  }
}

describe('MatchSfx.frame', () => {
  it('щит off→on: shield_up + startLoop; on→off: shield_down + stopLoop', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    sfx.frame([input({ shieldActive: false })])
    sfx.frame([input({ shieldActive: true })])
    expect(fake.played('shield_up')).toBe(1)
    expect(fake.calls.some(c => c.method === 'startLoop' && c.event === 'shield_loop')).toBe(true)
    sfx.frame([input({ shieldActive: false })])
    expect(fake.played('shield_down')).toBe(1)
    expect(fake.calls.some(c => c.method === 'stopLoop')).toBe(true)
  })

  it('рывок false→true → dash (один раз)', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    sfx.frame([input({ dashing: false })])
    sfx.frame([input({ dashing: true })])
    sfx.frame([input({ dashing: true })])
    expect(fake.played('dash')).toBe(1)
  })

  it('justJumped → jump и возвращает move-jump', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    const moves = sfx.frame([input({ justJumped: true })])
    expect(fake.played('jump')).toBe(1)
    expect(moves).toEqual([{ id: 1, kind: 'jump', pos: expect.anything() }])
  })

  it('grounded false→true → land', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    sfx.frame([input({ grounded: false })])
    sfx.frame([input({ grounded: true })])
    expect(fake.played('land')).toBe(1)
  })

  it('cooldown_ready — когда рывок ИЛИ щит перешёл в готов (только локальный)', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    sfx.frame([input({ dashReady: false, shieldReady: true })])
    sfx.frame([input({ dashReady: true, shieldReady: true })])   // рывок стал готов
    expect(fake.played('cooldown_ready')).toBe(1)
  })

  it('move(): озвучивает прыжок/приземление соперника', () => {
    const fake = new FakeSfxEngine(); const sfx = new MatchSfx(fake)
    sfx.move('jump', new THREE.Vector3())
    expect(fake.played('jump')).toBe(1)
  })
})
```

- [ ] **Step 2: Запустить — упадёт**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts`
Expected: FAIL (`frame`/`move`/`PlayerSfxInput` нет).

- [ ] **Step 3: Реализовать frame()/move() в MatchSfx**

Append to `src/game/audio/sfx/MatchSfx.ts` (внутри класса добавить поля/методы, и экспортировать тип):

```ts
export type MoveKind = 'jump' | 'land'

export interface PlayerSfxInput {
  id: number
  obj: THREE.Object3D
  pos: THREE.Vector3
  shieldActive: boolean
  dashing: boolean
  grounded: boolean | null      // null = неизвестно (удалённый на клиенте) → jump/land приходят событием
  justJumped: boolean
  dashReady: boolean | null     // не-null только для локального игрока (cooldown_ready)
  shieldReady: boolean | null
}

interface PrevState { shield: boolean; dashing: boolean; grounded: boolean | null; dashReady: boolean; shieldReady: boolean }
```

В классе добавить поле и методы:

```ts
  private prev = new Map<number, PrevState>()

  /** Перекличка состояний за кадр: щит/рывок/прыжок/land/cooldown. Возвращает движения для эмита (host). */
  frame(inputs: PlayerSfxInput[]): { id: number; kind: MoveKind; pos: THREE.Vector3 }[] {
    const moves: { id: number; kind: MoveKind; pos: THREE.Vector3 }[] = []
    for (const inp of inputs) {
      const prev = this.prev.get(inp.id)
      if (inp.shieldActive && !(prev?.shield)) {
        this.engine.playAt('shield_up', inp.pos)
        this.engine.startLoop('shield_loop', `shield:${inp.id}`, inp.obj)
      } else if (!inp.shieldActive && prev?.shield) {
        this.engine.playAt('shield_down', inp.pos)
        this.engine.stopLoop(`shield:${inp.id}`)
      }
      if (inp.dashing && !(prev?.dashing)) this.engine.playAt('dash', inp.pos)
      if (inp.justJumped) { this.engine.playAt('jump', inp.pos); moves.push({ id: inp.id, kind: 'jump', pos: inp.pos.clone() }) }
      if (inp.grounded === true && prev?.grounded === false) { this.engine.playAt('land', inp.pos); moves.push({ id: inp.id, kind: 'land', pos: inp.pos.clone() }) }
      if (inp.dashReady !== null && inp.shieldReady !== null && prev) {
        if ((inp.dashReady && !prev.dashReady) || (inp.shieldReady && !prev.shieldReady)) this.engine.play2D('cooldown_ready')
      }
      this.prev.set(inp.id, {
        shield: inp.shieldActive, dashing: inp.dashing, grounded: inp.grounded,
        dashReady: inp.dashReady ?? true, shieldReady: inp.shieldReady ?? true,
      })
    }
    return moves
  }

  /** Прыжок/приземление соперника (клиент, событие move) — позиционно. */
  move(kind: MoveKind, pos: THREE.Vector3): void { this.engine.playAt(kind, pos) }

  /** Очистка лупов/состояния (конец матча). */
  reset(): void {
    for (const id of this.prev.keys()) this.engine.stopLoop(`shield:${id}`)
    this.prev.clear()
  }
```

- [ ] **Step 4: Запустить — зелёный**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts`
Expected: PASS.

- [ ] **Step 5: Body.justJumped + Player.justJumped**

In `src/game/Body.ts`:

(a) добавить публичное поле рядом с `grounded = true`:
```ts
  justJumped = false   // прыжок применён в этом кадре (для SFX); живёт один кадр
```
(b) в `stepJump()` выставлять флаг в обеих ветках прыжка:
```ts
  stepJump() {
    this.jumpedThisFrame = false
    this.justJumped = false
    const edge = this.jumpHeld && !this.prevJumpHeld
    if (this.grounded && this.jumpHeld) {
      this.velocityY = JUMP_FORCE
      this.airJumps = MAX_AIR_JUMPS
      this.jumpedThisFrame = true
      this.justJumped = true
    } else if (edge && !this.grounded && this.airJumps > 0) {
      this.velocityY = JUMP_FORCE
      this.airJumps--
      this.justJumped = true
    }
    this.prevJumpHeld = this.jumpHeld
  }
```

In `src/game/Player.ts` добавить геттер (рядом с `get grounded()`):
```ts
  get justJumped()        { return this.body.justJumped }
```

- [ ] **Step 6: Событие `move` в протоколе**

In `src/net/protocol.ts` в `MatchEvent` добавить вариант:
```ts
  | { t: 'move';  id: number; kind: 'jump' | 'land'; pos: Vec3 }
```

- [ ] **Step 7: Wiring в Match — host (оба игрока + emit move), client (свой + соперник из снапшота + applyEvent move), отсчёт**

In `src/game/Match.ts`:

(a) Хелпер сборки входа и эмита движений — добавить приватный метод:
```ts
  private sfxFrameHost() {
    if (!this.sfx) return
    const inputs = this.players.map(p => ({
      id: p.id, obj: p.bodyGroup, pos: p.position,
      shieldActive: p.shieldActive, dashing: p.dashing, grounded: p.grounded, justJumped: p.justJumped,
      dashReady: p.id === this.localId ? p.dashCooldownProgress() >= 1 : null,
      shieldReady: p.id === this.localId ? p.shieldProgress() >= 1 : null,
    }))
    const moves = this.sfx.frame(inputs)
    for (const m of moves) this.emit({ t: 'move', id: m.id, kind: m.kind, pos: toVec3(m.pos) })
  }
```
Вызвать в host-ветке `update` после `this.controllers.forEach(c => c.lateUpdate?.(dt))` (строка ~236), т.е. в самом конце host-обновления (после applyPhysics — `grounded`/`justJumped` уже свежие).

(b) **Client (свой игрок):** добавить приватный метод и вызвать в client-ветке `update` после `this.applyPhysics(dt)` (~строка 221):
```ts
  private sfxFrameClientSelf() {
    if (!this.sfx) return
    const me = this.human
    this.sfx.frame([{
      id: me.id, obj: me.bodyGroup, pos: me.position,
      shieldActive: me.shieldActive, dashing: me.dashing, grounded: me.grounded, justJumped: me.justJumped,
      dashReady: me.dashCooldownProgress() >= 1, shieldReady: me.shieldProgress() >= 1,
    }])
  }
```
Вызов: в client-ветке `update` после `this.applyPhysics(dt)` добавить `this.sfxFrameClientSelf()`.

(c) **Client (соперник из снапшота):** в `applySnapshot` (цикл ~507-513), в ветке `else p.applyNetState(ps)` добавить детект щита/рывка соперника:
```ts
      else {
        p.applyNetState(ps)
        this.sfx?.frame([{
          id: ps.id, obj: p.bodyGroup, pos: p.position,
          shieldActive: ps.shieldActive, dashing: ps.dashing, grounded: null, justJumped: false,
          dashReady: null, shieldReady: null,
        }])
      }
```

(d) **Client (прыжок/land соперника):** в `applyEvent` добавить кейс:
```ts
      case 'move': {
        if (e.id === this.localId) break   // своё движение уже сыграно предсказанием
        this.sfx?.move(e.kind, this.byId.get(e.id)?.position ?? fromVec3(e.pos))
        break
      }
```

(e) **Отсчёт (`count_tick`):** добавить поле `private prevCountTick = 0` (рядом с другими полями) и в `tickPhase()` (где известна фаза/отсчёт) добавить детект смены целой секунды. Найди в `tickPhase` вычисление секунд отсчёта (там, где `phase === 'countdown'` и `countdownEndsAt`). Добавь:
```ts
    if (this.phase === 'countdown') {
      const left = Math.ceil((this.countdownEndsAt - Date.now()) / 1000)
      if (left !== this.prevCountTick && left >= 1 && left <= 3) this.sfx?.play2D('count_tick')
      this.prevCountTick = left
    } else {
      this.prevCountTick = 0
    }
```
(этот код исполняется и на host, и на client — оба проходят фазу `countdown` локально; `count_tick` 2D, сети не нужно.)

(f) **Конец матча:** в `endMatch` рядом с `this.music?.fadeOut()` добавить `this.sfx?.reset()` (гасит луп щита).

- [ ] **Step 8: Проверки**

Run: `npx vitest run --config vitest.config.ts tests/unit/MatchSfx.test.ts tests/unit/Match.sfx.test.ts tests/unit/Match.music.test.ts` → PASS.
Run: `npx tsc -b --noEmit` → без ошибок.

- [ ] **Step 9: Коммит**

```bash
git add src/game/Body.ts src/game/Player.ts src/net/protocol.ts src/game/audio/sfx/MatchSfx.ts src/game/Match.ts tests/unit/MatchSfx.test.ts
git commit -m "feat(sfx): движения (прыжок/рывок/щит/land/cooldown), событие move, отсчёт"
```

---

## Task 6: UI-звуки (тоглы/готов/лобби) + баланс + верификация

**Files:**
- Modify: `src/screens/Settings.tsx`, `src/screens/Lobby.tsx`, `src/net/LobbySession.ts` (или место входа соперника), `src/components/ReadyOverlay.tsx` (или где кнопка «Готов»)
- Test: `tests/sfx.spec.ts` (e2e-смоук)

- [ ] **Step 1: ui_toggle на переключателях настроек/карт**

В `src/screens/Settings.tsx` и в местах с сегментами/плитками (`.seg`, `.map-tile`) при клике-переключении вызвать `useSfx().play2D('ui_toggle')`. Конкретно: в каждом обработчике выбора сегмента/плитки добавить `sfx.play2D('ui_toggle')` (взять `const sfx = useSfx()` в компоненте). Кнопки `<Button>` уже звучат `ui_click` (Task 3) — для тоглов используем `ui_toggle`, чтобы не дублировать.

- [ ] **Step 2: ready и lobby_join**

- «Готов»: в обработчике кнопки готовности (`ReadyOverlay`/там, где зовётся `requestReady`) добавить `useSfx().play2D('ready')`.
- Вход соперника в лобби: в `LobbySession` при заполнении слота соперника (там, где `opponent` становится занят человеком/ботом) сообщить наверх — проще всего в `App`/`Lobby`, где отображается появление соперника: при переходе слота из пустого в занятый вызвать `sfx.play2D('lobby_join')`. Реализация: в `Lobby.tsx` через `useEffect` на изменение наличия соперника:
```ts
  const sfx = useSfx()
  const hadOpponent = useRef(false)
  useEffect(() => {
    const has = /* признак занятого слота соперника из props/view */ Boolean(opponent)
    if (has && !hadOpponent.current) sfx.play2D('lobby_join')
    hadOpponent.current = has
  }, [opponent, sfx])
```
(подставь реальное выражение наличия соперника из текущих props `Lobby`.)

- [ ] **Step 3: e2e-смоук SFX**

Create `tests/sfx.spec.ts`:

```ts
import { test, expect } from './fixtures'
import { waitForGame } from './helpers'

test('SFX: движок грузится, матч стартует и стрельба не роняет аудио', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', e => errors.push(String(e)))
  await page.goto('/')
  await waitForGame(page, { difficulty: 'passive' })
  // дать матчу пожить и выстрелить
  await page.mouse.click(640, 360)
  await page.waitForTimeout(500)
  expect(errors.join('\n')).not.toMatch(/sfx|audio|AudioListener|PositionalAudio/i)
})
```

- [ ] **Step 4: Прогон смоука**

Run: `npx playwright test --project=headless tests/sfx.spec.ts`
Expected: PASS (нет аудио-ошибок; движок грузится, attach к камере работает).

- [ ] **Step 5: Баланс громкости (на слух — владелец)**

Константы в `src/game/audio/sfx/ThreeSfxEngine.ts`: `SFX_MASTER_GAIN`, `SFX_GAIN` (per-event), `SFX_REF_DISTANCE`/`SFX_ROLLOFF`/`SFX_MAX_DISTANCE`. Стартовые значения заданы; финальная подстройка — по фидбеку владельца (UI тише геймплея; лупы умеренные).

- [ ] **Step 6: Полная верификация**

Run: `npx tsc -b --noEmit` → без ошибок.
Run: `npm run lint` → без ошибок.
Run: `npm run test` → все юниты + e2e зелёные (возможны предсуществующие физические flaky — не по SFX; проходят на ретрае).

- [ ] **Step 7: Сверка со спекой**

Пройтись по `docs/superpowers/specs/2026-06-08-game-sfx-design.md`: движок/проводка, карта триггеров (17 звуков; ghost_loop не подключён), сетевая модель, UI/отсчёт/cooldown, тесты. Закрыть пробелы при наличии.

- [ ] **Step 8: Коммит**

```bash
git add -A
git commit -m "feat(sfx): UI-звуки (тоглы/готов/лобби) + e2e-смоук + баланс"
```

---

## Заметки по граблям

- **Два AudioContext** (музыка отдельным движком + three-listener для SFX) сосуществуют; оба резюмятся по жесту (клик в меню). Это ок.
- **`erasableSyntaxOnly`:** без enum/parameter-properties; поля классов объявляй явно.
- **Свой vs соперник:** свои действия озвучиваются из локальной симуляции (без задержки); соперник — боёвка/прыжок/land из событий, щит/рывок из флагов снапшота. Клиент игнорит свои эхо-`move` (id == localId).
- **ghost_loop** не подключаем (нет состояния «ghost form»; фаза `respawning` — кратковременный призрак, озвучивать не просили).
- **Лупы** держим по ключу `shield:<id>`; `MatchSfx.reset()` на конце матча гасит их. `engine.detach()` (анмаунт Game) тоже останавливает все лупы.
- **Коммиты:** сообщения однострочные (`-m`) — безопасно в любом шелле.
```
